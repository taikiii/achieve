<div class="container">
  <div class="row">
    <h2><%= @user.name %>フォローユーザー一覧</h2>
    <%= render 'followed_user' %>
    </div>
  <div class="row">
    <h2><%= @user.name %>フォロワーユーザー一覧</h2>
    <%= render 'followers_user' %>
   </div>
</div>
